version: "3.3"

services:
  redis:
    container_name: redis
    build: ./redis
    ports:
      - "6379:6379"
    volumes:
      - ./redis/redis-data:/data
    environment:
      - REDIS_PASSWORD=redis
    healthcheck:
      test: exit 0

  channel-db:
    container_name: channel-db
    build: ./channel-db
    ports:
      - "5432:5432"
    volumes:
      - ./channel-db/pg-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    healthcheck:
      test: exit 0

  message-db:
    container_name: message-db
    build: ./message-db
    ports:
      - "27017:27017"
    volumes:
      - ./message-db/mongo-data:/data/db
    environment:
      - MONGO_USERNAME=mongo
      - MONGO_PASSWORD=mongo
    healthcheck:
      test: exit 0

  channel-service:
    container_name: channel-service
    build: ./channel-service
    ports:
      - "3001:3000"
    volumes:
      - "./channel-service:/usr/src/app"
      - "./channel-service/package.json:/usr/src/package.json"
    environment:
      - NODE_ENV=${NODE_ENV}
      - SECRET=\x02\xf3\xf7r\t\x9f\xee\xbbu\xb1\xe1\x90\xfe
      - DATABASE_URL=postgres://postgres:postgres@channel-db:5432/channel_dev
      - DATABASE_TEST_URL=postgres://postgres:postgres@channel-db:5432/channel_test
      - REDIS_URL=redis://:redis@redis:6379/0
      - SMTP_URL=smtp://marty.barrows92@ethereal.email:hT8tSyK6VUuvBfVDGH@smtp.ethereal.email:587/?pool=true
    depends_on:
      - channel-db
      - redis
    links:
      - channel-db
      - redis

  message-service:
    container_name: message-service
    build: ./message-service
    ports:
      - "3002:3000"
    volumes:
      - "./message-service:/usr/src/app"
      - "./message-service/package.json:/usr/src/package.json"
    environment:
      - NODE_ENV=${NODE_ENV}
      - SECRET=\x02\xf3\xf7r\t\x9f\xee\xbbu\xb1\xe1\x90\xfe
      - DATABASE_URL=mongo://message-db:27017/message_dev
      - DATABASE_TEST_URL=mongo://message-db:27017/message_test
      - REDIS_URL=redis://:redis@redis:6379/0
    depends_on:
      - message-db
      - redis
    links:
      - message-db
      - redis

  call-service:
    container_name: call-service
    build: ./call-service
    ports:
      - "3003:3000"
    volumes:
      - "./call-service:/usr/src/app"
      - "./call-service/package.json:/usr/src/package.json"
    environment:
      - NODE_ENV=${NODE_ENV}
      - SECRET=\x02\xf3\xf7r\t\x9f\xee\xbbu\xb1\xe1\x90\xfe
      - REDIS_URL=redis://:redis@redis:6379/0
    depends_on:
      - redis
    links:
      - redis

  file-service:
    container_name: file-service
    build: ./file-service
    ports:
      - "3004:5000"
    volumes:
      - "./file-service:/usr/src/app"
      - "./file-service/requirements.txt:/usr/src/requirements.txt"
    environment:
      - SECRET=\x02\xf3\xf7r\t\x9f\xee\xbbu\xb1\xe1\x90\xfe

  nginx:
    container_name: nginx
    build: ./nginx
    ports:
      - 80:80
    volumes:
      - "./nginx:/etc/nginx/conf.d"
      - "./file-service/app/static:/files"
    depends_on:
      - channel-service
      - message-service
      - call-service
      - file-service
    restart: always
    healthcheck:
      test: exit 0
    command: "/bin/sh -c 'while :; do sleep 6h & wait $${!}; nginx \
      -s reload; done & nginx -g \"daemon off;\"'"